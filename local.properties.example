## Android SDK path (Android Studio default shown)
sdk.dir=/path/to/Android/sdk

## License secret used to generate and verify buyer license keys.
## Keep this value private — do not commit to git.
## Must match the value used in generate_key.sh.
# licenseSecret=your_secret_here
